{
  "name": "wp-modules-tools-config",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "js-yaml": "^4.1.0"
  }
}
